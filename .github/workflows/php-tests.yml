name: PHP Tests

on:
   push:
      branches: [main, dev]
   pull_request:
      branches: [main, dev]

jobs:
   test:
      runs-on: ubuntu-latest
      steps:
         - name: Checkout repository
           uses: actions/checkout@v4

         - name: Install build dependencies
           run: sudo apt-get update && sudo apt-get install -y build-essential cmake

         - name: Configure compressor (Release)
           run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

         - name: Build compressor binary
           run: cmake --build build --target compressor -j 8

         - name: Copy compressor into src/bin
           run: |
              mkdir -p src/bin
              cp build/compressor src/bin/compressor

         - name: Setup PHP
           uses: shivammathur/setup-php@v2
           with:
              php-version: "8.2"
              extensions: curl
              coverage: none

         - name: Validate composer.json and composer.lock
           run: composer validate --no-check-all --strict

         - name: Autoload all classes with Composer
           run: composer dump-autoload

         - name: Run tests
           run: composer test
